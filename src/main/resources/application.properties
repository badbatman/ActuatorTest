# Server configuration
server.port=8080

# Actuator configuration has been moved to:
# - ActuatorEndpointAutoConfiguration (defaults)
# - CustomHealthEndpointAutoConfiguration (health indicator)
#
# Override defaults in this file if needed:
# management.endpoints.enabled-by-default=true
# management.endpoint.health.enabled=false

# Debug logging for troubleshooting
logging.level.org.springframework.boot.actuate=DEBUG
logging.level.org.springframework.web.servlet.handler=DEBUG
