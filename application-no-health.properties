# Test Configuration: NO Health Endpoint Enabled

# This simulates having NO health endpoint at all
management.endpoints.enabled-by-default=false
management.endpoint.health.enabled=false

# Try to expose health, but it won't work because it's disabled above
management.endpoints.web.exposure.include=health
